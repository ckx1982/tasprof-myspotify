using Microsoft.VisualStudio.TestTools.UnitTesting;
using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using Tasprof.Components.SpotifyChart.Repositories;

namespace Tasprof.Components.SpotifyChart.Core.Tests.Repositories
{
    [TestClass]
    public class UnitOfWorkTest : BaseRepositoryTest
    {

        [TestMethod]
        public void MyTestMethod()
        {
            //UnitOfWork unitOfWork = new UnitOfWork(session);

            //Assert.IsTrue(session.IsOpen);

            //unitOfWork.BeginTransaction();

            //Assert.IsTrue(session.IsOpen);
            //var tracks = unitOfWork.SpotifyChartTracksRepository.Get().ToList();
           
            //unitOfWork.Commit();

            //Assert.IsTrue(session.IsOpen);

            //Assert.AreEqual(1, tracks.Count);
        }
    }
}
